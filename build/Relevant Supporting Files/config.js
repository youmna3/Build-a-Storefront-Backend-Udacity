"use strict";
/*import dotenv from 'dotenv';

dotenv.config();

const { PORT, NODE_ENV, DB_HOST, DB_PORT, DB_DATABASE, DB_DATABASE_TEST, DB_USER, DB_PASSWORD } =
  process.env;

export default {
  port: PORT,
  host: DB_HOST,
  dbport: DB_PORT,
  database: NODE_ENV === 'dev' ? DB_DATABASE : DB_DATABASE_TEST,
  user: DB_USER,
  password: DB_PASSWORD
};
*/
